import styled from '@emotion/styled';

type AdminBarProps = {};

export default function AdminBar({}: AdminBarProps) {
  return <Container>ADmin</Container>;
}

const Container = styled.div`
  height: 20px;
  background-color: red;
`;
